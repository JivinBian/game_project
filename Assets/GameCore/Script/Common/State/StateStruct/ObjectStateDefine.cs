namespace GameCore.Script.Common.State.StateStruct
{
    public enum ObjectStateType
    {
        State,
        Action
    }
    public enum ObjectEnvironment
    {
        Land
    }
    public enum ObjectState
    {
        Stand,
        Run,
        Coma
    }

    public enum ObjectAction
    {
        Idle,
        Collect
    }
}